generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model property {
  id    Int     @id @default(autoincrement())
  key   String  @unique
  value String
}
